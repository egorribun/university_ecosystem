[project]
name = "university-ecosystem"
version = "1.0.0"
description = "University Ecosystem Platform"
requires-python = ">=3.11"
dependencies = [
    "aiofiles>=23.0,<24.0",
    "aiosqlite>=0.19",
    "alembic>=1.13,<2.0",
    "anyio>=4.0.0,<5.0",
    "argon2-cffi>=23.1.0",
    "asyncpg>=0.30.0",
    "bcrypt==5.0.0",
    "boto3>=1.34.0,<2.0.0",
    "brotli-asgi>=1.4",
    "clamd>=1.0.2",
    "cryptography>=41",
    "defusedxml>=0.7.1",
    "diskcache>=5.6.0",
    "elasticsearch[async]>=8.0,<9.0.0",
    "email-validator>=2.0",
    "fastapi>=0.128,<1.0",
    "filelock>=3.20.3",
    "geoip2>=4.0",
    "gputil>=1.4.0",
    "grpcio>=1.60.0",
    "grpclib>=0.4.7",
    "httpx>=0.27",
    "minio>=7.2",
    "nats-py>=2.9",
    "opentelemetry-api",
    "opentelemetry-exporter-otlp",
    "opentelemetry-instrumentation",
    "opentelemetry-instrumentation-asgi",
    "opentelemetry-instrumentation-asyncpg",
    "opentelemetry-instrumentation-fastapi",
    "opentelemetry-instrumentation-httpx",
    "opentelemetry-instrumentation-redis",
    "opentelemetry-instrumentation-sqlalchemy",
    "opentelemetry-sdk",
    "opentelemetry-semantic-conventions",
    "orjson>=3.10",
    "pgvector[sqlalchemy]>=0.4.2",
    "pillow>=10",
    "pillow-avif-plugin>=1.4.0",
    "polars>=1.0",
    "prometheus-client>=0.20",
    "protobuf>=4.25.0",
    "psutil>=6.0.0",
    "psycopg[binary]>=3.1",
    "pyasn1>=0.6.2",
    "pydantic>=2,<3",
    "pydantic-settings>=2,<3",
    "pyjwt>=2.10.1",
    "pyotp>=2.9",
    "pyroscope-io>=0.8.0 ; sys_platform != 'win32'",
    "python-dotenv>=1.0",
    "python-magic>=0.4.27",
    "python-multipart>=0.0.22",
    "pyvips>=2.2 ; sys_platform != 'win32'",
    "pywebpush>=1.9",
    "redis>=5,<6",
    "sentry-sdk[opentelemetry]>=2,<3",
    "sqlalchemy[asyncio]>=2,<3",
    "starlette>=0.40",
    "strawberry-graphql[fastapi]>=0.250",
    "structlog>=24.0",
    "taskiq>=0.11.0",
    "taskiq-fastapi>=0.3.0",
    "taskiq-redis>=1.0.0",
    "typer>=0.12.0",
    "urllib3>=2.6.3",
    "uvicorn[standard]>=0.23",
    "uvloop>=0.21 ; sys_platform != 'win32'",
    "webauthn>=2.0.0",
    "zxcvbn>=4.5.0",
    "authzed>=1.15.0",
    "grpcio-health-checking>=1.60.0",
    "dishka[fastapi]>=0.14",
]

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_any_generics = true
no_implicit_optional = true
strict_equality = true
show_error_codes = true
pretty = true
explicit_package_bases = true

# Per-module overrides for gradual adoption
[[tool.mypy.overrides]]
module = ["alembic.*", "tests.*"]
disallow_untyped_defs = false
check_untyped_defs = false

# Routers and services contain many FastAPI route handlers decorated with
# Depends(); their signatures are validated by FastAPI at startup rather than
# mypy.  Allow missing annotations in these modules during the migration period.
[[tool.mypy.overrides]]
module = [
    "app.routers.*",
    "app.api.auth.*",
    "app.api.internal.*",
    "app.services.*",
    "app.repositories.*",
]
disallow_untyped_defs = false

[[tool.mypy.overrides]]
module = ["pyotp", "pywebpush", "brotli_asgi", "PIL.*", "pyvips"]
ignore_missing_imports = true

[tool.ruff]
target-version = "py311"
line-length = 88
exclude = ["gen"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
    "RUF", # ruff-specific rules
]
ignore = [
    "E501",   # line too long
    "B008",   # do not perform function calls in argument defaults (FastAPI standard)
    "B007",   # loop control variable not used
    "RUF001", # ambiguous characters
    "RUF002",
    "RUF003",
    "RUF012", # mutable class default (too many for now)
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["ARG001", "ARG002", "E402", "W291", "S101", "ERA001"]
"alembic/*" = ["I001", "E402"]
"app/main.py" = ["E402"]

[tool.ruff.lint.isort]
known-first-party = ["app"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
filterwarnings = [
    "error::DeprecationWarning",                # our code: treat as errors
    "ignore::DeprecationWarning:passlib",       # passlib/bcrypt upstream noise
    "ignore::DeprecationWarning:pkg_resources", # setuptools legacy noise
    "ignore::PendingDeprecationWarning",        # third-party pending removals
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks as integration test requiring external services",
    "flaky: marks as potentially flaky test",
    "security: marks as security-related test",
]
addopts = [
    "--strict-markers",
    "-ra",              # Show extra test summary for all except passed
]

[tool.coverage.run]
relative_files = true
source = ["app"]
branch = true

[tool.coverage.report]
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if __name__ == .__main__.:",
    "raise AssertionError",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
]

[dependency-groups]
dev = [
    "asgi-lifespan>=2.1.0",
    "fakeredis>=2",
    "hypothesis>=6.112",
    "mypy>=1.11",
    "pre-commit>=3.8",
    "bandit[toml]>=1.8",
    "pytest>=8.3",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5",
    "pytest-xdist>=3.5",
    "ruff>=0.15",
]

[tool.bandit]
# Bandit static analysis security scanner.
# Runs via pre-commit on app/ (excluding tests/).
targets = ["app"]
exclude_dirs = ["tests", "alembic"]
skips = [
    # B101: assert — acceptable in non-production guard code.
    "B101",
    # B104: bind all interfaces — uvicorn does this intentionally (0.0.0.0).
    "B104",
]
