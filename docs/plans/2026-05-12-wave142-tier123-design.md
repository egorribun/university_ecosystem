# Wave 142 Tier 1+2+3 broad — design

**Date**: 2026-05-12
**Branch**: `egorribun`
**Base HEAD**: `33fc7cc04` (W141 polish-v3, 2026-05-11)
**Scope**: NO-DEPLOY continued (W125–W141 SSR migration arc + local + structural)
**Wall-clock budget**: ~16–25h (Tier 1+2+3 broad per user Q1; expects 5–10 (z) discoveries per Q3 open-ended absorption)
**Expected §Honesty caveat band post-W142**: 4–7 (pre-W142: 7–8)

---

## Context

W141 closed with **0 caveats CLOSED at runtime + 5 NEW (z) discoveries** collapsed into structural W142+ scope tracking (per `docs/audits/AUDIT_WAVE141.md`). Two hypotheses were honestly disproved via CI/runtime verification:

1. **W141 SW1 axe coverage via `VITE_E2E_MODE=1`** — local build verified flag PROPAGATION (`data-e2e-stub` markers in dist; tree-shake invariant preserved). BUT CI run `25698785125` (12m23s success, 2026-05-11T21:50Z) produced 8/8 routes with `axeError: "axe-analyze-timeout-60s"`. The W116 SW1 reduced-MainLayout strips CHROME weight, but route CONTENT (DashboardHero + cards + maplibre-gl + charts + lists + Framer Motion) still triggers the 60s `Promise.race` timeout in `wave138-visual-audit.mjs`. **W140 NEW caveat #5 stays OPEN as W142 Tier 1 #1 target**.

2. **W141 SW3-SW5 Path (a-auth) full Temporal token validation** — 5 (z) cascades revealed `temporalio/auto-setup` is structurally incompatible with auth-on-init. SW5b rolled back docker-compose temporal block to W139 SW2 `admin-tools:1.30.2` baseline while **preserving 100% of the W141 SW3-SW5 scaffolding** (PowerShell `New-TemporalServiceToken`, file-processor Go credentials, `.secrets/` mount, `FP_TEMPORAL_API_KEY_FILE` env). **W141 §Honesty #6 reclassified to W142 Tier 1 #2 target**.

W141 polish A3 also surfaced a build-infra non-determinism gap previously masked by W140 polish A7's `ls -la` file-size method: defensive bundle rebuild × 2 produces main JS + server.js BYTE-IDENTICAL sha256 BUT `_shell.html` + `sw.js` have SAME byte count with DIFFERENT sha256. **W142 Tier 2 #2 target** (THE FLOOR — guaranteed close per low cascade risk).

W142 closes the remaining structural debt with **three parallel attacks** plus a guaranteed-improvement floor.

---

## User-approved scope (3-question AskUserQuestion 2026-05-12)

- **Q1**: Tier 1+2+3 broad (~16–25h) — full breadth
- **Q2**: Phase 1 Explore agent first — RECOMMENDED, completed via 3 parallel Explore agents
- **Q3**: Open-ended absorption — W139/W140/W141 pattern; structural deferrals acceptable per `feedback_perfectionism.md`

---

## Phase 1 Explore findings synthesis (3 parallel agents, 2026-05-12)

### Agent 1 (axe coverage paths): Path C recommended (70-85% success)

- **Path C** (content render reduction under VITE_E2E_MODE) — ~10–20 LoC across 3 files; extends [MainLayout.tsx:30](frontend/src/components/layout/MainLayout.tsx:30) pattern down one level
- Target heavy components:
  - `/dashboard` (HEAVIEST): DashboardHero + DashboardBackdrop + WeatherAmbient + 8 stories carousel + tilt hooks
  - `/activity` (2nd): ActivityBackdrop canvas + ActivityTrendChart + ActivityBarChart + ActivityHeatmap
  - `/map` (3rd): MapLibreMapComponent WebGL + WeatherParticles canvas + MapBackdrop
- **Context7 findings**: `@axe-core/playwright` advanced API:
  - `setLegacyMode(true)` halves memory (single injection vs default dual) — already used in `a11y-public.spec.ts` for WebKit
  - `withTags([...])` filters AFTER engine eval (not before) — doesn't reduce engine load
  - `include(selector)` happens DURING DOM walk (Context7 confirmed: "Test specific elements only" still walks full axe algorithm on included scope)
  - **NO documented "lite" / "minimal" preset** exists in axe-core — Path D collapses to Path B
- Fallback: Path B (`.include("main")` + `.disableRules`) 20-30% additional probability; ~5-10 LoC delta on top of Path C
- Phased iter approach: Iter 1 /dashboard → Iter 2 /activity → Iter 3 /map → optional Iter 4 Path B fallback

### Agent 2 (Path a-auth): Path A pre-bootstrap recommended (~6-8h)

- **W141 scaffolding verified 100% intact** with line-number citations:
  - [start-docker.ps1:150-218](start-docker.ps1:150) — `New-TemporalServiceToken` (RS256, sub=file-processor-service, aud=temporal, 1y exp, idempotent)
  - [services/file-processor/internal/config/config.go:20,66](services/file-processor/internal/config/config.go:20) — `TemporalAPIKeyFile` field + FP_TEMPORAL_API_KEY_FILE env binding
  - [services/file-processor/cmd/file-processor/main.go:154-201](services/file-processor/cmd/file-processor/main.go:154) — `connectTemporal()` reads file + `NewAPIKeyStaticCredentials(token)` + `ConnectionOptions.TLSDisabled: true`
  - [docker-compose.full.yml:440](docker-compose.full.yml:440) — `./.secrets:/app/.secrets:ro` mount
  - [docker-compose.full.yml:432](docker-compose.full.yml:432) — `FP_TEMPORAL_API_KEY_FILE: ""` (empty; W142 SW5 activates)
  - `.secrets/temporal_api_key` + `.secrets/jwt_rs256.pem` both exist (verified via Glob)
- **W141 (z) #1-#4 still relevant**: auto-setup tag drift (1.29.6.1 latest), USE_INTERNAL_FRONTEND required, auto-setup namespace registration uses public frontend (auth-gated, incompatible), worker SDK internal addr context deadline
- **CRITICAL pre-impl gap**: Context7 docs show `temporal --api-key <string>` flag (TLS-auto when present) BUT NOT `--header "Authorization: Bearer <token>"` inline flag. SW2 must verify the actual CLI flag syntax for header-based auth at runtime BEFORE SW3 compose swap (per W141 anti-pattern #2 — runtime swap IS verification, not Context7 schema alone)
- **Path A** chosen over Path B (plain server + manual schema) or Path C (two-phase auth toggle) because:
  - Leverages preserved W141 scaffolding (lowest marginal cost)
  - Closest production-K8s parity (managed Temporal Cloud bootstrap pattern)
  - Single-phase config (no SIGHUP/restart fragility)

### Agent 3 (Tier 2 candidates): #2 INCLUDE, #1 DEFER

- **Tier 2 #1 LHCI gate ratchet** — DEFER to W143+
  - PAGE_HUNG (W139 (z) #10) structurally blocked; root cause undiagnosed
  - 2-3h diagnosis + unknown structural fix (~5-10h cascade risk)
  - Possible causes: SSR prerender content complexity, Lighthouse throttling on TanStack Start SPA shell, CSP nonce placeholder injection
  - W124 SW4 baseline (3 sessions × 3 runs / + /dashboard medians 0.43/0.44 CLS 0.033) — no measurement post-SSR migration
  - W120 SW2 gate values (CLS error@0.10, Perf relaxed warn@0.40 in routine-e5) preserved
  - **OPTIONAL polish-pass spike** (1-2h max) to surface 1-day-fix vs architectural debt
- **Tier 2 #2 build-infra non-determinism** — **INCLUDE** in W142 (~3.5-4h)
  - Likely sources: `post-build-shell.mjs` regex match order + `workbox-build.injectManifest` iterates `fs.readdir` in OS-dependent order; precache manifest entries lack explicit sort
  - PRIMARY fix at [build-orchestrated.mjs:373-378](frontend/scripts/build-orchestrated.mjs:373): post-process workbox manifest sort
  - Low cascade risk; THE FLOOR for W142 (guaranteed close even if Tier 1 cascades)

---

## SW sequence (SW0 → SW7 + optional polish)

| SW | Title | Est. time | Files | LoC | Deviation trigger |
|----|-------|-----------|-------|-----|-------------------|
| SW0 | Design + memory pre-flight | 15-20 min | design doc + memory | ~250-350 | Scope mismatch → AskUserQuestion |
| SW1 | Axe Path C content render reduction | 3-5h phased | Dashboard.tsx, ActivityFeature.tsx, MapFeature.tsx | ~10-20 | Path C insufficient → Path B fallback iter 2; >4 CI iters → defer to W143+ |
| SW2 | Path (a-auth) CLI flag verification | ~1h READ-ONLY | NO commits (design append) | 0 | Fundamental gap → defer SW3-5, SW6 still ships |
| SW3 | docker-compose temporal swap | 1-2h | docker-compose.full.yml | ~20-30 YAML | NEW (z) discovery → SW5b |
| SW4 | admin-tools sidecar namespace register | 1-2h | docker-compose.full.yml NEW block | ~30-50 YAML | Token rejected 3 iters → SW5b |
| SW5 | file-processor FP_TEMPORAL_API_KEY_FILE activation | 1-2h | docker-compose.full.yml line 432 | ~2-5 YAML | "Connected to Temporal" absent → SW5b |
| SW5b | Honest rollback (CONDITIONAL) | — | docker-compose.full.yml revert | — | SW3/4/5 deviation OR >6h cumulative |
| SW6 | Build-infra non-determinism (THE FLOOR) | 3.5-4h | build-orchestrated.mjs, post-build-shell.mjs, workbox-config.mjs | ~30-60 | 3rd build still drifts → defer to W143+ structural |
| SW7 | Audit + memory + N+3 rotation + W143 prompt | 1-1.5h | AUDIT_WAVE142.md, CLAUDE.md, INDEX.md, memory | — | — |
| OPTIONAL polish | LHCI PAGE_HUNG diagnosis spike (READ-ONLY) | 1-2h max | findings appended to audit | 0 | >2h no actionable finding → defer entirely |

**Total estimate**: ~13-17h core + 1-2h optional polish = ~14-19h. Within Q1 range "~16-25h" with margin for (z) cascades per Q3.

---

## Critical files map (file_path:line_number references)

**Tier 1 #1 axe Path C**:
- [frontend/src/pages/Dashboard.tsx](frontend/src/pages/Dashboard.tsx) — gate DashboardHero + Backdrop + stories + tilt hooks
- [frontend/src/features/activity/ActivityFeature.tsx](frontend/src/features/activity/ActivityFeature.tsx) — gate ActivityBackdrop + 3 charts
- [frontend/src/features/map/MapFeature.tsx](frontend/src/features/map/MapFeature.tsx) — gate maplibre-gl lazy import + WeatherParticles
- Pattern source: [frontend/src/components/layout/MainLayout.tsx:30](frontend/src/components/layout/MainLayout.tsx:30) (`const E2E_MODE = import.meta.env.VITE_E2E_MODE === "1"`)
- CI: [.github/workflows/visual-audit.yml](.github/workflows/visual-audit.yml)
- Audit script: [frontend/scripts/wave138-visual-audit.mjs](frontend/scripts/wave138-visual-audit.mjs) (Path B fallback site)

**Tier 1 #2 Path (a-auth)**:
- [docker-compose.full.yml](docker-compose.full.yml) ~585–649 (temporal block — SW3 full rewrite)
- [docker-compose.full.yml](docker-compose.full.yml) ~425–447 (file-processor block — SW5 env + depends_on)
- [docker-compose.full.yml](docker-compose.full.yml) NEW block (~25–35 LoC) for `temporal-admin-tools-sidecar` (SW4)
- [start-docker.ps1:150-218](start-docker.ps1:150) — UNCHANGED, consumed
- [services/file-processor/internal/config/config.go:20,66](services/file-processor/internal/config/config.go:20) — UNCHANGED, activated
- [services/file-processor/cmd/file-processor/main.go:154-201](services/file-processor/cmd/file-processor/main.go:154) — UNCHANGED, exercises auth path

**Tier 2 #2 build-infra non-determinism**:
- [frontend/scripts/build-orchestrated.mjs:373-378](frontend/scripts/build-orchestrated.mjs:373) — PRIMARY fix
- [frontend/scripts/post-build-shell.mjs:74-86](frontend/scripts/post-build-shell.mjs:74) — verify CSP nonce regex deterministic
- [frontend/scripts/workbox-config.mjs](frontend/scripts/workbox-config.mjs) — explanatory comment

---

## Verification matrix

| Metric | Pre-W142 (HEAD `33fc7cc04`) | Post-W142 best case | Post-W142 worst case (SW5b) |
|---|---|---|---|
| tsc errors | 0 | 0 | 0 |
| eslint `--max-warnings=0` | 0 | 0 | 0 |
| vitest single + cross-session 5-run | 1052p/12s/0f flake band 0 | unchanged | unchanged |
| i18n parity | 18/18 | 18/18 | 18/18 |
| npm audit | 0 vulnerabilities | 0 vulnerabilities | 0 vulnerabilities |
| Cargo.lock drift | none (≥31 waves idempotent) | none | none |
| Bundle BYTE-IDENTICAL × 3 sha256 | 2 of 4 artifacts drift | **4/4 BYTE-IDENTICAL** (SW6) | **4/4 BYTE-IDENTICAL** (SW6 floor) |
| Tree-shake `lhci-mock-user` PROD | 0 | 0 | 0 |
| Tree-shake `data-e2e-stub` PROD | 0 (E2E off) | 0 | 0 |
| Tree-shake `data-e2e-stub` E2E build | present (chrome stubs) | present (chrome + content stubs) | present (Path C added) |
| SW IIFE invariant | `"use strict";(()=>{` | unchanged | unchanged |
| Docker `temporal` health | (healthy) admin-tools:1.30.2 | (healthy) auto-setup post-sidecar | (healthy) rolled back |
| Docker `temporal-admin-tools-sidecar` | absent | (healthy) | absent |
| Docker `file-processor` health + token | (healthy), "no auth" log | (healthy), "Attached service token" log | (healthy), "no auth" log |
| NATS streams TASK_QUEUE + FILES_PROCESS | provisioned (W140 SW1) | unchanged | unchanged |
| W141 Go scaffolding | present, inactive | present, ACTIVE | present, inactive (preserved) |
| CI visual-audit.yml axe coverage | 0/8 routes valid | **≥ 5/8 valid** | 3-5/8 (Path B fallback) |
| §Honesty caveat count | 7-8 | **4-5** | 6-7 (honest deferral) |

---

## §Honesty trajectory projection (per `feedback_perfectionism.md`)

**Pre-W142 caveat count**: 7-8 (post-W141 polish-v3 revised)

**Caveat inventory carried into W142**:
1. W134 #2 bundle delta (recording-only, superseded) — UNCHANGED
2. W134 #10 /messenger Phase 5 punted (Tier 5) — UNCHANGED
3. W137 #5 file-processor temporal-localhost — **PARTIAL → W142 SW3-5 closure target**
4. W137 #6+#7 by-design dev-only — UNCHANGED (acceptable)
5. W140 NEW healthcheck override dev-only — UNCHANGED (correct override)
6. W140 NEW #5 axe coverage 0/8 Linux CI heavy DOM — **W142 SW1 closure target**
7. W140 NEW #6 Path (a-auth) — **W142 SW3-5 closure target (combined with #3)**
8. W141 (z) #1-#4 collapsed sub-discoveries → W142 Path (a-auth) scope tracking
9. W141 polish A3 build-infra non-determinism — **W142 SW6 closure target (THE FLOOR)**

**Post-W142 best case** (all Tier 1+2 close): ~4-5 caveats
- ✅ #3 + #7 + #8 collapsed: CLOSED (Path A pre-bootstrap; SW3-SW5 land)
- ✅ #6: CLOSED (Path C + CI verification ≥ 5/8 routes)
- ✅ #9: CLOSED (SW6 sha256 BYTE-IDENTICAL × 3)
- ⚠️ #1, #2, #4, #5: UNCHANGED (recording / by-design / Tier 5)

**Post-W142 worst case** (SW5b cascade + Path C insufficient even with Path B): ~6-7 caveats. **Still honest improvement** because:
- ✅ #9: CLOSED via SW6 floor (low cascade risk)
- ⚠️ #6: PARTIAL via Path B fallback (≥ 3/8 routes coverage)
- ⚠️ #3 + #7: re-deferred to W143+ structurally (scaffolding preserved per W141 pattern)
- Documentation-debt caveats closed via SW0+SW7 audit hygiene

---

## Anti-pattern reminders (carry-forward from W138-W141 + NEW)

1. **W141 anti-pattern #1 (CRITICAL)**: CI verification REQUIRED in SW1, not deferred to polish-pass. SW1 budget: 3 CI iters (one per route) + 1 fallback = 4 max. W140 anti-pattern #1 (don't iterate CI > 5-8 times) applies.
2. **W141 anti-pattern #2**: Runtime swap IS the verification step for SW3 (and SW4, SW5). Context7 schema verification alone is insufficient. W142 SW2 is read-only verification; SW3-SW5 IS the runtime swap.
3. **W141 anti-pattern #3**: Agent reports can be 1+ wave stale. W142 Phase 1 Explore prompts required date/commit citations; verify at SW execution time anyway.
4. **W141 anti-pattern #4**: Don't claim "Path X closes §Honesty Y" pre-implementation. Closure claims wait for SW7 audit with actual evidence.
5. **W141 polish-v3 NEW lesson**: A flag taking effect (tree-shake invariant passing) is NOT the same as the flag CLOSING the wall it was designed for. SW1 verifies downstream wall-test outcome (axe analyze finishes ≥ 5/8 routes), not upstream flag-propagation invariant.
6. **W137 SW4 / W140 (z) #5 path-typo class**: SW1 `data-e2e-stub` attribute naming + SW3 env var names (`SKIP_DEFAULT_NAMESPACE_CREATION` etc.) MUST be verified against actual upstream docs/source before commit. SW2 is the verification gate for SW3 env var names.

---

## Plan deviation triggers (single-source register)

| Trigger | Action |
|---|---|
| SW1 iter 1 (/dashboard) Path C insufficient — timeout persists | Path B fallback iter 2 BEFORE moving to /activity |
| SW1 Path B at iter 2 still fails on /dashboard | Defer axe wall to W143+ structurally |
| SW1 CI iter count > 4 total | Defer remaining routes/fallback to W143+ |
| SW2 temporal CLI flag verification reveals no `--header` | Re-evaluate Path A vs B vs C; if no viable → defer SW3-5, SW6 still ships |
| SW3 auto-setup logs NEW (z) discovery | Document as (z) #N; if mitigation < 1h attempt; otherwise SW5b |
| SW4 sidecar token rejected 3 iters | Verify claim shape vs config_template.yaml; >1h debug → SW5b |
| SW5 `Connected to Temporal` absent after correct env + sidecar healthy | SW5b honest rollback |
| Cumulative SW3-SW5 wall-clock > 6h | SW5b regardless of partial progress |
| SW6 3rd-build sha256 still drifts | Locate ACTUAL drift source; <1.5h additional → fix; else defer to W143+, document root cause |
| Polish-pass LHCI PAGE_HUNG diagnosis > 2h | Defer entire ratchet to W143+ |

---

## Reusable patterns from existing code (DO NOT rebuild)

- **W116 SW1 reduced layout** at [MainLayout.tsx:30](frontend/src/components/layout/MainLayout.tsx:30) — `E2E_MODE` constant + landmark stub pattern (`{!E2E_MODE && <Heavy />}` + `{E2E_MODE && <stub />}`)
- **W141 SW4 service token mint** at [start-docker.ps1:150-218](start-docker.ps1:150) — `New-TemporalServiceToken` UNCHANGED, consumed by SW4 sidecar
- **W141 SW5 Go credentials attach** at [main.go:154-201](services/file-processor/cmd/file-processor/main.go:154) — `connectTemporal` UNCHANGED, exercised by SW5
- **W140 polish A7 sha256 methodology** — `Get-FileHash -Algorithm SHA256` cross-comparison; SW6 elevates polish-pass × 2 to in-wave × 3 BYTE-IDENTICAL
- **W138 SW2 esbuild IIFE format** at [build-orchestrated.mjs:330](frontend/scripts/build-orchestrated.mjs:330) — `format: "iife"` lock-in; SW6 keeps intact
- **W137 SW5/SW6 sidecar healthiness** at [docker-compose.full.yml:513-518](docker-compose.full.yml:513) — `grpc_health_probe` sidecar pattern; SW4 borrows shape
- **W140 SW3 depends_on chain** at [docker-compose.full.yml:441-461](docker-compose.full.yml:441) — extended in SW5 with `temporal-admin-tools-sidecar: service_healthy`

---

## Anticipated post-wave polish budget (per `feedback_perfectionism.md`)

W141 had 3 polish rounds (~90 min total). W142 baseline: anticipate **2 rounds** (~60 min):

- **Round 1**: standard 30-60 min polish-pass (cross-session vitest 5-run + bundle × 2 + commit shortstats + memory ref drift + start-docker.ps1 syntax + Docker stack health + NATS streams)
- **Round 2** (conditional): if CI verification of W142 SW1 axe-coverage closure DISPROVES the hypothesis (per W141 lesson #1), trigger polish-v2 honest revision similar to W141 polish-v3

If W141-style 3 rounds: budget ~90 min. If 2 rounds: ~60 min.

---

## References

- Plan file (ephemeral): `C:\Users\egorribun\.claude\plans\c-users-egorribun-claude-projects-c-use-cozy-blum.md`
- Memory backlog: `memory/wave142_backlog.md` (in `.claude` profile only)
- Opening prompt: `memory/wave142_opening_prompt.md` (in `.claude` profile only)
- Previous wave audit: `docs/audits/AUDIT_WAVE141.md`
- W137 §Honesty #5 origin: `docs/audits/AUDIT_WAVE137.md` (rotated to archive in W140 SW7)
- N+3 rotation candidate this wave: `docs/audits/AUDIT_WAVE139.md` → `archive/` (active becomes W140/W141/W142)
