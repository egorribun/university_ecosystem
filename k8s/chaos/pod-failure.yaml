apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: backend-pod-failure
spec:
  action: pod-failure
  mode: one
  selector:
    labelSelectors:
      app.kubernetes.io/name: backend
  duration: "30s"
