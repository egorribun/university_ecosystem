dist
node_modules
package-lock.json
coverage
.vite
dist-ssr
root
*.log

# W158 SW2 — TanStack Router auto-generated; regenerates on tsc/build/test.
# Format occasionally drifts from prettier's output (import order, trailing
# commas). Hit `npm run format:check` failures 5 consecutive waves W153-W157.
# This rule structurally closes the drift — prettier no longer checks the
# file. Cosmetic-only file consumed by TanStack Router runtime + tsc.
src/routeTree.gen.ts

# W179 SW1 — silence cargo target dirs + wasm-pack pkg dirs flagged by W178
# polish-v1 (z): `npx prettier --check .` from frontend/ flagged 245 pre-existing
# files. All in rust-crypto + wasm-sanitizer build outputs (cargo target/ +
# wasm-pack pkg/). lint-staged scopes to staged files in normal workflow so
# these never block commits, but standalone `prettier --check .` runs do.
**/target/**
**/pkg/**

# W180 SW2 — silence auto-generated artifacts + diagnostic dumps + SW placeholder
# flagged by W179 SW1 (z) finding (17 files). Categories:
# (a) Auto-generated by build pipeline — regenerated on every `npm run build`:
public/manifest*.webmanifest
public/static-shell-i18n.*
# (b) Service Worker placeholder bundled at build time (esbuild compiles
# src/sw.ts → dist/client/sw.js; this public/sw.js is bridge for dev-mode
# fallback per W138 SW2):
public/sw.js
# (c) Wave-specific diagnostic dump (W136 hang investigation artifact at
# `<repo>/frontend/.wave136-trace/`; gitignored via root `.gitignore:379`
# `.wave136-trace/` pattern — file stays local for forensic reference but
# bypasses prettier scope):
.wave136-trace/

# MSW auto-generated mock handlers and wrappers
src/tests/mocks/generated/

