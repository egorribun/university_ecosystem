/**
 * Admin Feature
 *
 * Handles admin panels: users, audit, notifications, feature flags.
 */

// Pages will be moved here during migration
// Currently pages are in @/pages/Admin*.tsx

// API
export * from "@/api/notifications"
