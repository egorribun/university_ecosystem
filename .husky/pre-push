#!/usr/bin/env sh

# W156 SW4 Tier 2 — fixed `--prefix root/frontend` → `--prefix frontend`
# (pre-W156 path was a stale doc placeholder that never resolved →
# pre-push typecheck silently skipped, matching the pre-commit bug in
# .husky/pre-commit). Same fix class.
npm --prefix frontend run typecheck
