title = "university ecosystem gitleaks configuration"

[allowlist]
description = "Allow generated dependency lockfiles; source and example files remain scanned"
paths = ["uv.lock", "package-lock.json", "go.sum"]
