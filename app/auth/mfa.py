# This file is superseded by the app/auth/mfa/ package.
# Python's import system prefers the package directory (mfa/) over this file (mfa.py)
# when both exist in the same parent directory.
# This file should never be imported; it exists only as a reminder that the
# implementation has moved.  It will be removed in the next release.
